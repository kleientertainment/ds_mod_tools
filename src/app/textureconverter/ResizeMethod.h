#pragma once

namespace ResizeMethod
{
	enum Type
	{
		Stretch,// changes the canvas size and stretches the image to fill it
		Paste,	// changes the canvas size but just pastes the image into it
	};
}

